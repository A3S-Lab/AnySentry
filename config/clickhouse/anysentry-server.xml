<?xml version="1.0"?>
<clickhouse>
    <!--
      This development stack runs inside a bounded container. Keep ClickHouse
      below the cgroup ceiling so merges and native allocations have headroom.
    -->
    <max_server_memory_usage>2415919104</max_server_memory_usage>

    <logger>
        <level>warning</level>
    </logger>

    <!--
      The default image persists trace-level server and per-processor telemetry.
      Those tables can grow much larger than AnySentry's own facts and create a
      permanent merge/OOM loop. Container logs and Docker metrics remain
      available for infrastructure diagnostics.
    -->
    <text_log remove="remove"/>
    <trace_log remove="remove"/>
    <processors_profile_log remove="remove"/>
    <part_log remove="remove"/>
    <metric_log remove="remove"/>
    <asynchronous_metric_log remove="remove"/>
    <query_thread_log remove="remove"/>
    <query_views_log remove="remove"/>

    <!-- Keep only a short query audit window for diagnosing slow API queries. -->
    <query_log>
        <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
    </query_log>
</clickhouse>
